{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "noEmitOnError": true,
    // Repo root: the build emits both apps/api/src and the shared db/ modules it imports.
    "rootDir": "../.."
  },
  "include": ["src", "../../db/schema", "../../db/client.ts"]
}
